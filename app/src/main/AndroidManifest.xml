<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fallingmaze2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".GameOverScreen"></activity>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"> <!--Fixes the phone in portrait mode -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChooseLevel"
            android:parentActivityName=".ChoosePlayer" />
        <activity
            android:name=".Options"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ChoosePlayer"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".GameScreen"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".theGame.MainGame"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>